# Copy to .env in the repo root and fill in values.
# The scraper loads this automatically via python-dotenv.

# Railway PostgreSQL connection string — get from Railway dashboard > your service > Variables
DATABASE_URL=postgresql://postgres:password@hostname.railway.internal:5432/railway

# Optional: reporting API key (only needed if running the API locally)
# REPORT_API_KEY=your-secret-key
