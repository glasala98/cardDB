[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: pure unit tests, no DB required
    db: requires DATABASE_URL (live PostgreSQL)
